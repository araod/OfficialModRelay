import { GetCallbackUrlRequest, GetCallbackUrlResponse, } from './externalendpoints.js';
export class ExternalEndpointsClientJSON {
    constructor(rpc) {
        this.rpc = rpc;
        this.GetCallbackUrl.bind(this);
    }
    GetCallbackUrl(request) {
        const data = GetCallbackUrlRequest.toJSON(request);
        const promise = this.rpc.request('devvit.plugin.externalendpoints.v1alpha.ExternalEndpoints', 'GetCallbackUrl', 'application/json', data);
        return promise.then((data) => GetCallbackUrlResponse.fromJSON(data));
    }
}
export class ExternalEndpointsClientProtobuf {
    constructor(rpc) {
        this.rpc = rpc;
        this.GetCallbackUrl.bind(this);
    }
    GetCallbackUrl(request) {
        const data = GetCallbackUrlRequest.encode(request).finish();
        const promise = this.rpc.request('devvit.plugin.externalendpoints.v1alpha.ExternalEndpoints', 'GetCallbackUrl', 'application/protobuf', data);
        return promise.then((data) => GetCallbackUrlResponse.decode(data));
    }
}
